from django.conf import settings
# from gensim.models import KeyedVectors

print("Loading Model...")
# model = KeyedVectors.load_word2vec_format(f"{settings.BASE_DIR}/GoogleNews-vectors-negative300.bin.gz", binary=True, limit=10000)
model = None
print("Model Loaded!")
